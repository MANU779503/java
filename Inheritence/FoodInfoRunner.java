class FoodInfoRunner {
    public static void main(String[] args) {
        FastFood fastFood = new FastFood();
        fastFood.name();
        fastFood.type();
        fastFood.price();
        fastFood.brand();
        fastFood.weight();
        fastFood.isVegetarian();
        fastFood.caloriesPerServing();
        fastFood.specialEdition();
        fastFood.alternateBrand();
        fastFood.energy();
    }
}
